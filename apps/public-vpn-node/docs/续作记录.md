# 2026-09-24 续作现场记录

> 最终 release-prep 更新（2026-09-24）：下文保留历史审查/验收流程。完整 GPL 正文现已从本地 reference/GPL-3.0.txt 补齐到 licenses/GPL-3.0.txt，旧的“许可证正文缺失”阻塞已解除。既有架构已由人工验收（依据任务交接）；本次未运行 Docker 或重做运行验收。最新离线结果及发布边界以 [测试报告](测试报告.md) 为准。

接续任务：20260924-031520-6B1736。用户说明上一任务因 600 秒上限超时、状态 failed、没有 After Snapshot；本次不把磁盘文件视为全新项目，也不声称能恢复缺失快照。

## 初检状态

| 文件/成果 | 接手时状态 | 本次处理 |
| --- | --- | --- |
| app/security.py、entrypoint.py | 已有凭据验证、配置白名单、入口防火墙先行 | 保留，补充调用链与故障测试 |
| app/firewall.py | 已有标记流量 output 阻断 | 增加 postrouting 检查、事务刷新自有表 |
| app/proxy_server.py | 已有强制认证、标记、tun0 绑定、禁止系统 DNS 回退 | 校验 DNS 回包来源/问题/标志、密码学随机 ID、仅 A 查询、限制线程并回收失败槽位、Unicode 错误认证安全拒绝 |
| app/vpngate_manager.py | 已有 API TLS 限制、凭据分离、日志抑制、配置检查 | 补缓存信任边界、路由失败中止、DCO 禁用、移除导入副作用与运行期 sysctl、限 Web 并发、关闭旧日志读取、修正 UI 行为/文案 |
| app/vpn_utils.py | 已移除 DNS 修复与归属查询 | 移除多余直连诊断及宿主风格服务/防火墙诊断 |
| Dockerfile、compose.yaml | 已有回环映射、最小权限、专用卷、只读 secret | 复核后保留原文件；未为了制造变更而重写 |
| .env.example、.gitignore | 已排除真实秘密、reference 和测试产物 | 保留；确认无真实值 |
| .dockerignore | 已有构建白名单 | 收紧 app/许可证为明确文件名；重新排除目录内容后仅放行所需文件，修复目录否定规则扩大构建范围的问题 |
| scripts/configure_credentials.py、healthcheck.py | 已有隐藏输入/独占创建与端口活性检查 | 保留、补文档与回归 |
| scripts/preflight.py、test_preflight.py | 16 项已有回归通过；配置静态检查 | 增强白名单、commit/许可证一致性和失败路径测试 |
| docs/源码审查.md、清单、指纹 | 已有固定源码审查材料 | 保留证据，补续作修正与准确边界；指纹未变 |
| licenses/UPSTREAM-LICENSE.txt、NOTICE.txt | 原样省略版 GPL 声明及缺口说明 | 验证原文一致；NOTICE 补修改记录 |
| README.md、docs/测试报告.md | 仍写“源码未获取”，与现场不符 | 更新为完整中文运维文档与本次实际测试结果 |

## 新增文件

- `scripts/static_checks.py`：明确公开文件白名单的敏感模式/权限/Dockerfile/构建排除/shell 检查。
- `scripts/verify.sh`：项目内编译与离线检查入口。
- `scripts/probe_proxy.py`：仅供人工运行的隐藏认证代理探测。
- `scripts/check_runtime_logs.py`：仅供人工运行的隐藏输入精确比对与日志白名单检查。
- `docs/人工Docker验收.md`：配置、构建、权限、TUN、监听、Web、代理、出口、DNS、故障、日志与恢复命令。
- 本记录。

修改文件为表内注明修正的 app 四模块、.dockerignore、preflight/test_preflight、源码审查报告、README、测试报告、NOTICE；另更新审查清单状态。保留原 Dockerfile/Compose/security/entrypoint/凭据工具/健康检查是有意选择，不表示漏查。Python 编译可能生成项目内忽略的缓存，不作为交付源码。

## 不变的边界

未运行 Docker/宿主安装器、未访问 daemon、未 sudo/提权、未改系统配置/用户组/其他项目、未读取真实 .env/secret/会话/密钥、未修改 reference、未联网取上游、未上传或 git push。未访问受保护的 Git 元数据，因此不声称确认暂存区为空；交由人工 `/diff` 核对交付范围。

固定 commit `14b803685e67b7d50deff8ee768d042d96dcaab1`。LICENSE 为 GPL-3.0-or-later 声明，但原文不完整；对外分发前须补交完整正文。当前可以进入批准后的本地隔离人工 Docker 验收，尚不具备业务上线或对外分发完成条件。
