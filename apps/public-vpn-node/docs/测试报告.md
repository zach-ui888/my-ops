# 最终 release-prep 离线测试报告

日期：2026-09-24。上游 commit：`14b803685e67b7d50deff8ee768d042d96dcaab1`。

## 本次结果

`bash scripts/verify.sh`：53 项 unittest 全部通过（test_preflight 28 项、test_vpn_runtime 25 项），无减少既有覆盖。Python compileall、项目 preflight、32 个公开文件 static checks、Shell 语法检查和 release checks 全部通过。

保留动态 TUN 识别、fail-closed kill switch、fwmark policy routing、健康检查、OpenVPN 断线自动恢复和 HTTP/SOCKS5 逻辑。app/ 仅 vpn_utils.py、vpngate_manager.py 清除行尾空白，无功能变更。测试使用合成凭据与 mock，临时文件和编译缓存仅写入项目 .test-work/。

## 许可证与空白

已读取本地官方完整 GPL v3 正文，逐字节复制到 licenses/GPL-3.0.txt，SHA-256：
`3972dc9744f6499f0f9b2dbf76696f2ae7ad8af9b23dde66d6af86c9dfb36986`。
上游省略版 licenses/UPSTREAM-LICENSE.txt 原样保留，NOTICE 明确 GPL-3.0-or-later、来源、修改日期和担保声明。未联网、未修改 reference/；原有 11 项上游指纹复核通过。完整正文进入 Docker 白名单。

对全部正式文件逐个执行 git diff --no-index --check，把整份文件视作新增内容，检查行尾空白、文件尾空行及 tab 前空格；错误为零。未关闭或弱化检查。新增 release_checks.py 纳入 verify.sh，持续校验完整正文指纹、空白及排除规则。

## Secret 与发布边界

公开文件白名单扫描通过；未读取真实 .env、secrets/、会话、运行数据或日志。静态检查包含已知令牌/私钥/认证 URL/硬编码敏感字段，不能证明任意未知格式秘密不存在。

.gitignore 排除 secrets/、reference/、.test-work/、data/、runtime/、日志、真实 .env、密钥与本地工具目录；.dockerignore 默认全部排除，仅允许运行源码、healthcheck、Dockerfile 和三个许可证文件。代表性禁止路径与必要许可文件检查通过，Dockerfile COPY 白名单保持不变。未运行 Docker 验证实际 context。

## After Snapshot 与 Approval

公开发布集合为 scripts/static_checks.py 的 PUBLIC 列表及 docs/After-Snapshot.sha256 本身；清单不包含自身哈希。清单覆盖全部正式源码、构建文件、测试、文档、示例配置与许可材料，不包含本地 reference 或运行产物。人工只导入该集合到 my-ops/apps/public-vpn-node，不整目录上传。

当前无 Git 仓库，git status/git ls-files 返回 not a git repository；因此未验证实际索引，也没有 commit/push。人工首次导入时须复核暂存文件与此清单一致。已满足进入人工 Approval 的离线条件。

既有人工验收状态依据任务交接，本次不冒充重新验证真实内核/TUN/nft/OpenVPN/出口/DNS/断线恢复。未执行 Docker、容器停止/重启、宿主网络/防火墙修改、提权或部署；未修改 TG Codex Agent。公共 VPN、控制面直连例外及未锁定依赖的既有安全边界继续适用。镜像分发时另须落实对应源码交付。
