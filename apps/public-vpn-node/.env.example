# 不含秘密；默认部署无需 .env。
# 以下为容器接口说明，已在 compose.yaml/entrypoint.py 固定，非密码变量。
# CREDENTIALS_FILE=/run/secrets/credentials
# VPNGATE_DATA_DIR=/data
# UI_PORT=8787
# LOCAL_PROXY_PORT=7928
# 管理员用 scripts/configure_credentials.py 在私密终端手工输入五项凭据。
# 不将密码放入环境变量、命令行、README 或本模板。
